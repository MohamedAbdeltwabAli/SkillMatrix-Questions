# دليل استخدام وتوثيق نظام "Skill Matrix"

هذا الملف يوضح هيكل النظام، المميزات، وطريقة العمل واستخدام كل جزء من أجزاء النظام الموجه لقياس مهارات ومعارف الموظفين وتحليل بياناتهم.

---

## 1. فكرة النظام والهدف منه
يهدف النظام إلى أتمتة عملية اختبار وتقييم موظفي الأقسام المختلفة من خلال:
- سحب أسئلة عشوائية من بنك الأسئلة بناءً على فئات مخصصة لكل قسم.
- منع الغش أو مشاركة الحسابات من خلال ربط حساب الموظف بجهاز واحد فقط (Device Fingerprinting).
- توفير لوحات تحكم للإدارة والمديرين لعرض نتائج الاختبارات وتحليل نقاط الضعف والقوة.

---

## 2. المكونات الرئيسية

### أ) شاشة الاختبار للموظف (Employee Portal)
- **تسجيل الدخول:** يقوم الموظف بتسجيل الدخول باستخدام الرقم الوظيفي (SAP) وكلمة المرور.
- **تأمين الجهاز:** عند أول تسجيل دخول ناجح، يتم ربط الجهاز الحالي بحساب الموظف. في حال حاول الموظف الدخول من جهاز آخر سيتم منعه.
- **الاختبار:**
  - يتم جلب عدد الأسئلة المحددة لقسم الموظف من الفئات التي تم إعدادها مسبقاً في شاشة الإدارة.
  - يوجد مؤقت زمني يتم حسابه بناءً على عدد الأسئلة.
  - بمجرد الانتهاء أو انتهاء الوقت، يتم تصحيح الاختبار تلقائياً ورفع النتيجة مباشرة إلى قاعدة البيانات.

### ب) لوحة الإدارة (Admin Dashboard)
اللوحة الشاملة لإدارة النظام، وتحتوي على عدة تبويبات:

1. **👥 الموظفون:**
   - عرض قائمة بجميع الموظفين وحالاتهم.
   - إمكانية إضافة، تعديل، إيقاف/تفعيل حساب، وفك الحظر عن الأجهزة للموظفين.
   - **رفع ملفات إكسل (Excel):** يمكنك إضافة الموظفين بالجملة من خلال ملفات إكسل. (ملاحظة: إذا قمت برفع ملف به بيانات موجودة مسبقاً، سيتم "تحديث" بيانات الموظف ولن يتم تكراره).

2. **❓ الأسئلة:**
   - إدارة بنك الأسئلة بالكامل وإضافة أسئلة "اختيار من متعدد" أو "صح وخطأ".
   - رفع ملفات إكسل لإضافة الأسئلة دفعة واحدة. (يتم تحديث السؤال إذا كان رقم السؤال موجوداً مسبقاً).

3. **🏭 الأقسام وإعدادات الاختبار:**
   - إضافة وحذف الأقسام.
   - **تكوين الاختبارات:** يتيح لك تحديد الفئات التي سيتم اختبار القسم فيها، وتحديد عدد الأسئلة المطلوبة لكل فئة عبر قائمة منسدلة. سيقوم النظام بسحب هذا العدد عشوائياً لكل موظف ينتمي للقسم.

4. **📊 نتائج الاختبارات:**
   - عرض جميع نتائج الموظفين.
   - إمكانية الفلترة حسب (القسم، حالة النجاح/الرسوب، أو التاريخ).
   - زر "عرض" لرؤية الإجابة التي اختارها الموظف مقارنة بالإجابة الصحيحة.
   - زر "حذف" لحذف نتيجة الموظف والسماح له بأداء الاختبار مرة أخرى إذا تطلب الأمر.

5. **📉 تحليل الأسئلة:**
   - شاشة تفاعلية لتحليل أداء الموظفين في الأسئلة.
   - رسوم بيانية توضح "نسبة النجاح لكل فئة" و "نسبة النجاح لكل قسم" بناءً على اختيارات الفلترة (القسم، الفئة، التاريخ).
   - عرض جدول تفصيلي يوضح كم مرة أُجيب السؤال بشكل صحيح أو خاطئ، ونسبة النجاح لكل سؤال.
   - زر "التفاصيل" بجوار كل سؤال يتيح لك رؤية كل موظف، ماذا اختار، والمحاولة، والنتيجة.

6. **🔐 المستخدمون:**
   - إدارة مستخدمي لوحة التحكم (المديرين والمسؤولين).

---

## 3. أسئلة شائعة وطريقة الاستخدام

- **ماذا لو قام 100 موظف بإجراء الاختبار في نفس الوقت؟**
  النظام مبني على معمارية سحابية قوية ومُحسنة. عملية تصحيح الاختبار وتخزين البيانات تتم بالكامل داخل خوادم قاعدة البيانات (PostgreSQL RPC) مما يمنع حدوث أي تعارض (Race Conditions) ويضمن أداءً مستقراً حتى مع وجود أعداد ضخمة من الموظفين يختبرون في نفس اللحظة.

- **ماذا يحدث عند رفع ملفات الموظفين أو الأسئلة؟ هل تُضاف كبيانات جديدة أم تستبدل القديمة؟**
  النظام يعتمد على مبدأ الـ Upsert، أي أنه يقوم بالتحديث إذا وجد أن الرقم التعريفي (SAP للموظف، أو Q_ID للسؤال) موجود مسبقاً، ويقوم بالإضافة كبيانات جديدة إذا لم يكن موجوداً.

- **كيف أسمح للموظف بإجراء الاختبار للمرة الثانية؟**
  من تبويب "نتائج الاختبارات"، قم بالبحث عن نتيجة الموظف واضغط على "حذف". سيقوم النظام بمسح نتيجته وإلغاء ارتباط جهازه القديم، مما يسمح له بالدخول من جديد وبدء المحاولة الثانية كأنه يختبر للمرة الأولى، وسيتم تسجيل المحاولة الجديدة كمحاولة رقم 2.

- **كيف يتم ضمان عدم مشاركة الحساب بين أكثر من موظف؟**
  عند تسجيل الدخول لأول مرة، يقوم النظام بإنشاء بصمة فريدة للجهاز متصفح الموظف ويحفظها بقاعدة البيانات. إذا تم تسجيل الدخول من جهاز آخر سيتم رفض الدخول تلقائياً. يمكن للإدارة "فك الحظر" عن الجهاز في حال قام الموظف بتغيير جهازه الفعلي.

---

## 4. إمكانيات التطوير المستقبلية
النظام حالياً في حالة ممتازة ومستقرة، ويؤدي الهدف الرئيسي منه (القياس والتحليل) بكفاءة عالية واحترافية. إذا رغبتم في تطوير النظام مستقبلاً يمكن إضافة:
- **تحميل التقارير بصيغة PDF:** وتوليد تقارير مرئية يمكن طباعتها مباشرة لاجتماعات الإدارة.
- **تتبع تقدم الموظف (Historical Tracking):** في حالة إجراء الموظفين للاختبارات بشكل دوري (كل شهر مثلاً)، يمكن إضافة رسوم بيانية توضح مستوى تقدم الموظف.
- **إرسال شهادات إلكترونية:** إصدار شهادة اجتياز تلقائية للموظفين الذين حققوا درجات عالية وإرسالها بالبريد.
